# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Data lokal — JANGAN pernah commit (binary = tidak bisa di-merge)
*.db
*.sqlite*

# Bobot model (download otomatis / hasil training — share via Drive/Colab, bukan git)
*.pt
*.pth
*.onnx

# Video & foto uji / dataset (share via Drive/Roboflow, bukan git)
*.mp4
*.avi
*.mov
*.mkv
dataset/
datasets/
runs/

# Crop hasil scan (unknown_crops) & data lokal lain — jangan pernah commit
data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Perantara render proposal (dibuat ulang oleh scripts/proposal_pdf.py)
docs/proposal/diagram/
docs/proposal/_proposal.html
docs/proposal/DRAFT-PROPOSAL.html
